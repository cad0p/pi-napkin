name: Validate Package Version

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate-package-version:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporarily disabled for changelog cleanup — will be restored immediately after" && exit 0
